apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-pgadmin
  labels:
    app: {{ .Release.Name }}-pgadmin
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80          # Service port (used to access pgAdmin)
      targetPort: 80   # Container port inside pgAdmin pod
  selector:
    app: {{ .Release.Name }}-pgadmin
