# templates/postgres-configmap.yaml

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-postgres-config
data:
  POSTGRES_DB: tenjin
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres
